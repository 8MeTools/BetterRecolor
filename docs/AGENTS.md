## 前提

`BTRC-Before.md`に書かれているものはGoogle Colabで動作するために作成したPythonプログラムです。`ipynb`ファイルと構成は同一です。
このプログラムは**単に動く**ものであり、コードの設計はやや冗長です。
私が初心者の頃にバイブコーディングを通して作成したものであるからです。

> 例えば、ファイルを削除するコードは使いまわしているため、関数化すれば完結にコードを書くことができます。

## 依頼

- `BTRC-Before.md`を確認して、冗長な部分について指摘してください。
- `ipynb`では最小限のコードだけにしたいため、処理を行う部分は`.py`としてモジュール分割を行い、自作ライブラリのように作成するフローを説明してください。

処理を行う部分を`.py`ファイルで管理できるメリットは以下の通りです。

- Colabを必要としないローカルPython環境でも使用できる。
- メンテナンスが容易で、Gitで`diff`の確認がしやすい(`.ipynb`はdiffが確認しづらい)

## 注意

- ファイルの形式上、`.ipynb`を読み取って編集することはせず、プレーンテキストの`BTRC-Before.md`を読み取ってください。
- 今回はモジュール分割を行うことを目的とするため、関数化することを除いた **リファクタリング(文字列変換処理の効率化)** などは行わないでください。
- あくまで実装のフローを示すだけであり、実装そのものは私が行います。
- 将来的には`i18n`(多言語対応)にも対応できる設計を心がけること。
- 実装フローの確認がユーザから得られたら、`.md`形式で実装フローを`./docs`に出力すること。
- `docs`配下の多言語対応を行う場合は、i18nice(https://github.com/solaluset/i18nice)を使用すること。
